CREATE TABLE GROUP  ( 
	GROUP_ID  	INTEGER GENERATED BY DEFAULT AS IDENTITY (START WITH 1, INCREMENT BY 1, CACHE 2) NOT NULL,
	GROUP_NAME	VARCHAR(256) NOT NULL,
	CREATED_BY	VARCHAR(255),
	CREATED_ON	TIMESTAMP,
	UPDATED_BY	VARCHAR(255),
	UPDATED_ON	TIMESTAMP,
	DELETED   	SMALLINT,
	APP_ID    	INTEGER,
	CONSTRAINT SQL140420153827010 PRIMARY KEY(GROUP_ID)
)

CREATE TABLE ROLE  ( 
	ROLE_ID   	INTEGER GENERATED BY DEFAULT AS IDENTITY (START WITH 1, INCREMENT BY 1, CACHE 2) NOT NULL,
	ROLE_NAME 	VARCHAR(256) NOT NULL,
	CREATED_BY	VARCHAR(255),
	CREATED_ON	TIMESTAMP,
	UPDATED_BY	VARCHAR(255),
	UPDATED_ON	TIMESTAMP,
	CONSTRAINT SQL140420153818190 PRIMARY KEY(ROLE_ID)
)


CREATE TABLE GROUP_ROLE  ( 
	GROUP_ROLE_ID	INTEGER GENERATED BY DEFAULT AS IDENTITY (START WITH 1, INCREMENT BY 1, CACHE 2) NOT NULL,
	GROUP_ID     	INTEGER NOT NULL,
	ROLE_ID      	INTEGER NOT NULL,
	CONSTRAINT SQL140420153302300 PRIMARY KEY(GROUP_ROLE_ID)
)

ALTER TABLE GROUP_ROLE
	ADD CONSTRAINT ROLE_ID_FK
	FOREIGN KEY(ROLE_ID)
	REFERENCES UMS.ROLE(ROLE_ID)
	ON DELETE NO ACTION 
	ON UPDATE NO ACTION 
	NOT ENFORCED 
	DISABLE QUERY OPTIMIZATION 

ALTER TABLE GROUP_ROLE
	ADD CONSTRAINT GROUP_ID_FK
	FOREIGN KEY(GROUP_ID)
	REFERENCES UMS.GROUP(GROUP_ID)
	ON DELETE NO ACTION 
	ON UPDATE NO ACTION 
	NOT ENFORCED 
	DISABLE QUERY OPTIMIZATION 

	
CREATE TABLE USER  ( 
	USER_ID   	INTEGER GENERATED BY DEFAULT AS IDENTITY (START WITH 1, INCREMENT BY 1, CACHE 2) NOT NULL,
	USER_NAME 	VARCHAR(256) NOT NULL,
	CREATED_BY	VARCHAR(255),
	CREATED_ON	TIMESTAMP,
	UPDATED_BY	VARCHAR(255),
	UPDATED_ON	TIMESTAMP,
	EMAIL     	VARCHAR(256) NOT NULL,
	PASSWORD  	VARCHAR(256),
	DELETED   	SMALLINT,
	ACTIVE    	SMALLINT,
	CONSTRAINT SQL140226110950650 PRIMARY KEY(USER_ID)
)


CREATE TABLE USER_GROUP  ( 
	USER_GROUP_ID	INTEGER GENERATED BY DEFAULT AS IDENTITY (START WITH 1, INCREMENT BY 1, CACHE 2) NOT NULL,
	USER_ID      	INTEGER NOT NULL,
	GROUP_ID     	INTEGER NOT NULL,
	CONSTRAINT SQL140422181347770 PRIMARY KEY(USER_GROUP_ID)
)

